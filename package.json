{
  "name": "netent",
  "version": "1.0.0",
  "description": "A test task for NetEnt",
  "main": "index.js",
  "scripts": {
    "test:unit": "jest --notify --config=jest.config.json",
    "test:e2e": "echo 'No e2e tests'",
    "lint": "eslint --ext .js,.spec.js app",
    "start": "node app/server/index.js 3000",
    "test": "npm run test:unit && npm run test:e2e",
    "qa": "npm run lint && npm run test"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "express": "^4.16.3"
  },
  "devDependencies": {
    "concurrently": "^3.6.0",
    "eslint": "^5.0.1",
    "eslint-config-google": "^0.9.1",
    "jest": "^23.2.0"
  }
}
